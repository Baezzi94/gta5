# 셋업 절차 (1회, 유저 콘솔 작업)

프로젝트 확정값:
- Supabase URL: `https://rhdgmbljvunnfwmpqvqs.supabase.co`
- 디코 Client ID: `1526505336272584714`

## 1. Supabase 프로젝트 ✅ 완료

- `blackout-intel` 생성됨. 보안 옵션 3개(Data API / auto expose / auto RLS) 전부 켬.

## 2. 디스코드 OAuth 앱

1. discord.com/developers/applications → 만든 앱 → 왼쪽 **OAuth2** 메뉴
2. **Redirects**에 정확히 이 주소가 있어야 함 (오타 주의! rhdgm"b"lj):
   `https://rhdgmbljvunnfwmpqvqs.supabase.co/auth/v1/callback`
3. Client Secret: 같은 OAuth2 화면의 **"Reset Secret"** 버튼 클릭 → 새 시크릿이 한 번만 표시됨 → 복사
   (디코는 시크릿을 다시 안 보여줌. 잃어버리면 또 Reset 하면 됨)

## 3. Supabase에 디코 연결

1. 브라우저에서 바로 열기:
   `https://supabase.com/dashboard/project/rhdgmbljvunnfwmpqvqs/auth/providers`
   (수동 경로: 왼쪽 사이드바 방패 아이콘 **Authentication** → **Sign In / Providers** → Auth Providers 목록)
2. 목록에서 **Discord** 클릭 → **Enable Sign in with Discord** 토글 켜기
   → Client ID / Client Secret 붙여넣기 → **Save**
3. URL 설정: `https://supabase.com/dashboard/project/rhdgmbljvunnfwmpqvqs/auth/url-configuration`
   - **Site URL**: `http://localhost:5173`
   - **Redirect URLs** → Add URL: `http://localhost:5173/**`
   - (Vercel 배포 후 `https://<vercel-domain>/**` 추가 + Site URL 교체)

## 4. Storage 버킷

- `https://supabase.com/dashboard/project/rhdgmbljvunnfwmpqvqs/storage/buckets`
- **New bucket** → 이름 `tip-photos` → **Private bucket** (Public 체크 해제) → 생성

## 5. 로컬 환경변수

- anon key 위치: `https://supabase.com/dashboard/project/rhdgmbljvunnfwmpqvqs/settings/api-keys`
  → **anon / public** 키 복사 (service_role 말고!)
- `갱활동/app/.env.local` 파일 생성 (git에 안 올라감):

```
VITE_SUPABASE_URL=https://rhdgmbljvunnfwmpqvqs.supabase.co
VITE_SUPABASE_ANON_KEY=<anon public 키>
```

## 6. 마이그레이션 실행

- `https://supabase.com/dashboard/project/rhdgmbljvunnfwmpqvqs/sql/new`
- `supabase/migrations/0001_init.sql` 내용 전체 붙여넣고 **Run**
- 새 쿼리로 `0002_rls.sql` 붙여넣고 **Run**
- Table Editor에서 테이블 7개 + categories 5행 확인

## 7. 첫 관리자 지정 (본인)

- `npm run dev` → http://localhost:5173 → 디코 로그인 + 신원등록 1회 완료 후,
- Table Editor → profiles → 내 행: `status = active`, `role = intel_chief` 로 직접 수정
- 이후 승인·계급 부여는 앱 [관리] 탭에서
